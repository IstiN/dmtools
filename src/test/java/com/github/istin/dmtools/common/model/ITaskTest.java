package com.github.istin.dmtools.common.model;

import org.junit.Test;
import static org.junit.Assert.*;
import org.mockito.Mockito;

public class ITaskTest {

    @Test
    public void testGetAuthor() {
        // TODO: Implement this test once a concrete implementation of ITask is available
        // ITask task = new ConcreteTaskImplementation();
        // IUser expectedAuthor = new User("AuthorName");
        // Mockito.when(task.getAuthor()).thenReturn(expectedAuthor);
        // assertEquals(expectedAuthor, task.getAuthor());
    }
}